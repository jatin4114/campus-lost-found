NODE_ENV=development
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Supabase PostgreSQL connection string (see docs/database.md once written)
DATABASE_URL=

# JWT secrets (set once auth is implemented)
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
